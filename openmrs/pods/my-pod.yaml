apiVersion: v1
kind : Pod
metadata:
  name: mypod
spec:
  hostNetwork: true
  containers:
    - name: my-first-container
      image: rsuresh/rsuresh_openmrs:1.0
      ports:
      - containerPort: 8080
        