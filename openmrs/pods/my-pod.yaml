apiVersion: v1
kind : Pod
metadata:
  name: mypod
spec:
  hostNetwork: true
  containers:
    - name: my-first-container
      image: rsuresh_openmrs
      ports:
      - containerPort: 8085
          protocol: TCP