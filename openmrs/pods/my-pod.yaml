apiVersion: v1
kind : Pod
metadata:
  name: mypod
spec:
  hostNetwork: true
  containers:
    - name: my-first-container
      image: nginx
      ports:
      - containerPort: 80
          protocol: TCP