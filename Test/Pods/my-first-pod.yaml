apiVersion: v1
kind : Pod
metadata:
  name: mypod
spec:
  #hostNetwork: true
  containers:
    - name: my-first-container
      image: apache2
      ports:
        - containerPort: 80
    
                  