apiVersion: v1
kind : Pod
metadata:
  name: mypod
spec:
  #hostNetwork: true
  containers:
    - name: my-first-container
      image: jenkins
      ports:
        - containerPort: 8080
    
                  